{
  "name": "shopify_git_integtarion",
  "version": "1.0.0",
  "description": "",
  "dependencies": {
    "webpack": "^5.90.3"
  },
  "devDependencies": {
    "copy-webpack-plugin": "^12.0.2",
    "del-cli": "^5.1.0",
    "webpack-cli": "^5.1.4"
  },
  "scripts": {
    "test": "test",
    "build": "webpack --mode=development --no-devtool --config webpack.config.js",
    "develop": "webpack --mode=development --no-devtool --config webpack.config.js",
    "watch": "npm run develop ",
    "clear:build": "del-cli build/**",
    "theme:pull": "shopify theme pull --environment env1",
    "theme:dev": "shopify theme dev --environment env1 --open"
  }
}
